import 'package:flutter/material.dart';

const smallPadding = 10.0;
const normalPadding = 15.0;
const largePadding = 20.0;
const xLargePadding = 25.0;
const xxLargePadding = 30.0;

const xSmallVerticalDistance = SizedBox(
  height: 5,
);
const smallVerticalDistance = SizedBox(
  height: 10,
);
const normalVerticalDistance = SizedBox(
  height: 20,
);
const largeVerticalDistance = SizedBox(
  height: 30,
);
const xLargeVerticalDistance = SizedBox(
  height: 35,
);

const dividerThickness = 0.7;

const tabBarHeight = 60.0;

const albumListPadding = EdgeInsets.all(largePadding);
const albumDetailsPadding = EdgeInsets.symmetric(horizontal: xxLargePadding);
const albumInteractionPadding = EdgeInsets.symmetric(vertical: largePadding);

const photoListTileTitleGap = largePadding;

const photoListTilePadding = EdgeInsets.all(0);

const albumListTileDistance = normalVerticalDistance;

const circularProgressIndicatorBoxConstraint =
BoxConstraints(maxWidth: 50, maxHeight: 50);

const tabIconMargin = EdgeInsets.only(bottom: 3);

const contactInfoTextFieldPadding = EdgeInsets.symmetric(
    vertical: 0, horizontal: smallPadding);

const contactInfoPadding = EdgeInsets.symmetric(
    vertical: 0, horizontal: normalPadding);